%h1 All Movies

%table#movies
  %thead
    %tr
      %th{:class => 'hilite'} Movie Title
      %th Rating
      %th= link_to "Release Date", sort_by_release_date_movies_path
      %th More Info
  %tbody
    - @movies.each do |movie|
      %tr
        %td= movie.title 
        %td= movie.rating
        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie_path(movie)

= link_to 'Add new movie', new_movie_path
